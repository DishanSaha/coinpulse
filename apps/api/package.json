{
  "name": "@coinpulse/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "dotnet run",
    "build": "dotnet build --configuration Release",
    "clean": "rm -rf bin obj"
  }
}
